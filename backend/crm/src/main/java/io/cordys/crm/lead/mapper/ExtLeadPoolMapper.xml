<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.cordys.crm.lead.mapper.ExtLeadPoolMapper">
    <select id="list" resultType="io.cordys.crm.lead.dto.LeadPoolDTO" parameterType="io.cordys.common.dto.BasePageRequest">
        select * from lead_pool
        where organization_id = #{orgId}
        <include refid="filters">
            <property name="filters" value="request.filters"/>
        </include>
        <include refid="io.cordys.common.mapper.CommonMapper.sort">
            <property name="sort" value="request.sort"/>
            <property name="prefix" value=""/>
            <property name="defaultSort" value="create_time desc"/>
        </include>
    </select>

    <update id="updatePickRule" parameterType="io.cordys.crm.lead.domain.LeadPoolPickRule">
        update lead_pool_pick_rule set limit_on_number = #{rule.limitOnNumber}, pick_number = #{rule.pickNumber}, limit_pre_owner = #{rule.limitPreOwner},
                                           pick_interval_days = #{rule.pickIntervalDays}, update_user = #{rule.updateUser}, update_time = #{rule.updateTime}
        where pool_id = #{rule.poolId}
    </update>

    <update id="updateRecycleRule" parameterType="io.cordys.crm.lead.domain.LeadPoolRecycleRule">
        update lead_pool_recycle_rule set expire_notice = #{rule.expireNotice}, notice_days = #{rule.noticeDays},
                                              operator = #{rule.operator}, `condition` = #{rule.condition}, update_user = #{rule.updateUser}, update_time = #{rule.updateTime}
        where pool_id = #{rule.poolId}
    </update>

    <sql id="filters">
        <if test="${filters} != null and ${filters}.size() > 0">
            <trim prefix="and">
                <foreach collection="${filters}" item="condition" open="(" close=")" index="i" separator="and">
                    <if test="condition.name == 'auto'">
                        <include refid="io.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="lead_pool.lead_pool"/>
                        </include>
                    </if>
                    <if test="condition.name == 'enable'">
                        <include refid="io.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="ead_pool.enable"/>
                        </include>
                    </if>
                </foreach>
            </trim>
        </if>
    </sql>
</mapper>